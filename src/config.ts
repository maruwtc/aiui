const config = {
    // Used by the server-side API route to communicate with Ollama
    ollama_server_url: "https://ollama.maruwtc.com/api/chat",

    // Used by the client to communicate with Next.js API
    api_url: "/api/nextchat"
};

export default config;